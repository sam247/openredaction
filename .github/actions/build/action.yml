name: Build
description: Run turbo build for a workspace
inputs:
  filter:
    description: Turbo package filter (e.g. openredaction)
    required: true
runs:
  using: composite
  steps:
    - name: Build
      run: npx turbo run build --filter=${{ inputs.filter }}
      shell: bash
